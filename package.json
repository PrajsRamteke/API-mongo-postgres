{
  "name": "mongodb-postgresql-apis",
  "version": "1.0.0",
  "description": "Express applications for MongoDB and PostgreSQL APIs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/mongoapi.js --omit=dev || node src/postgresapi.js --omit=dev",
    "prod-install": "npm install --omit=dev"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "pg": "^8.11.3"
  }
}
